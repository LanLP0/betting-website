# Basic rabbitmq config
loopback_users.guest = false
listeners.tcp.default = 5672
management.tcp.port = 15672

# Metrics and tracing (useful for management)
# rabbitmq_prometheus plugin can be enabled if needed
